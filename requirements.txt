scipy>=1.7.0
matplotlib>=3.4.0
pandas>=1.3.0
scikit-learn>=0.24.0
mne>=0.24.0
PyQt5>=5.15.0
PyQtWebEngine>=5.15.0

# P300 Experiment Application — dependencies
# Install with: pip install -r requirements.txt

# Core scientific
numpy>=1.21.0

# Lab Streaming Layer — EEG marker streaming to LabRecorder
pylsl>=1.16.0

# Optional: frame-locked timing (auto-detected at runtime)
# Uncomment to install:
psychopy>=2023.1.0